import Container from "../lib/Container";
import Enemy from "./Enemy";

const EnemySpawner = (params) => {
  const { spawnPosition } = params;
  let spawnRate = 0.5; //seconds
};

export default EnemySpawner;
